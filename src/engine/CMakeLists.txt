add_plugin_library(engine
    PRIVATE_LIBS
        inferrt_cvcuda
        yaml-cpp::yaml-cpp
        TensorRT::TensorRT
    PUBLIC_LIBS
        CUDA::cudart
        opencv_core
        opencv_imgproc
        inferrt_core
        inferrt_util
        inferrt_model
)
