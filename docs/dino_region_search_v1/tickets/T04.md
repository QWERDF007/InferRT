# T04 · 区域/局部粗选

目标：双路扫描保留高召回候选并合并去重。

源码落点：`DinoScan.*`, `DinoSimilarity.*`, `DinoTopK.*`, `DinoFusion.*`。

实施：区域和局部描述独立相似度扫描，各使用 profile 候选配额；合并候选时保留来源与坐标，按图像/空间重叠去重，不让单路覆盖另一通道。空候选是合法结果，索引错误仍返回 error。

验收：真实正例和困难负例上报告粗选召回、候选数及两路贡献，覆盖矩形/多边形、尺度、边界和重复纹理。

依赖：T03。非范围：发布阈值、SHA。