Use case: infographic-diagram. Create ONE clean, understandable illustrated principle explainer, 16:9 wide landscape, high resolution, Chinese labels. Topic: DINO image-region search, offline compact index and online coarse selection followed by fine matching. This is a conceptual illustration, no charts, no performance numbers, no accuracy percentages, no promises, no marketing badges. Visual style: restrained educational editorial illustration on warm white background, crisp readable Chinese typography, simple flat illustrations of grey metal parts, dark navy text, muted teal for whole-region evidence, warm orange for small local detail. Concrete example throughout: a circular metal ring with a small triangular notch on its right edge. Show the same ring at different sizes in varied image thumbnails, and a distractor ring with no notch. Main title exactly: “用 DINO 在图库中寻找相似区域”. Top narrow band labeled “建库：图片只预处理一次”. Show image thumbnails, overlapping crops, a simple DINO feature-grid illustration, then compact stacks of colored descriptor cards. Short labels: “多尺度切片”, “DINO 特征”, “相近特征合并，差异局部保留”, “紧凑索引”. In the feature-grid illustration use many pale similar grid cells grouped together but one small orange notch cell visibly remains separate; do not show all cells discarded. Bottom main story labeled “搜索：先粗选，再精细匹配”. At left a larger source photo illustration of the ring with a visible orange rectangle around the ring and notch, label “框选查询区域”. In the middle clearly show TWO concurrent coarse-evidence branches: upper teal miniature ring silhouette labeled “看整体结构”; lower orange magnified notch labeled “看局部细节”. Both branches feed a small set of candidate image crops, label “合并候选区域”. To the right show magnified corresponding ring/notch crops connected by a few restrained feature correspondence lines, label “只对候选重算并精细匹配”. At far right show two original image thumbnails each with an orange box around a similar ring of a different size, label “返回原图中的匹配框”. Connect the top compact index to the coarse selection area using a single subtle downward connector. Keep a coherent left-to-right reading order, balanced whitespace, clear object examples more prominent than machinery, no code, no dense prose, no invented metrics, no claims like ‘zero loss’ or ‘guaranteed recall’, no dashboard gauges, no trophies, no neon gradients. Exact text must be legible, include all key labels, avoid extra paragraphs.

EDIT PROMPT
Edit this infographic, preserving the ring examples, two horizontal sections, all arrows and overall layout. Make only these corrections: (1) The entire 16:9 canvas, including the title margin and spaces outside panels, must have a solid opaque warm-white background. No transparency anywhere, no black background or cut-out edges. (2) Replace the label under the teal descriptor stack with exactly “整体区域描述”, no parenthetical claim about merging. (3) Replace the label under the orange descriptor stack with exactly “局部代表描述”. (4) The label under fine matching must read exactly “只对候选重算并精细匹配”. Keep the title and other labels clear and readable, no invented performance or accuracy numbers. Return a complete full infographic, not just the edited text.
